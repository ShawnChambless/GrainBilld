var app = angular.module('personalProject');

app.service('mainService', function($http, $q) {

    // this.grainSearch = function(grainSearchText) {
    //     return $http({
    //         method: 'GET',
    //         url: 'http://localhost:8080/front/ingredients/grain?' + grainSearchText
    //     });
    // };

});
